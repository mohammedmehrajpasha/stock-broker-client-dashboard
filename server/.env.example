# Port the API + WebSocket server listens on
PORT=4000
# Origin allowed by CORS / Socket.IO (the Vite dev server)
CLIENT_ORIGIN=http://localhost:5173
# Secret used to sign login JWTs (set a strong value in production)
JWT_SECRET=dev-secret-change-me
